target/

# Distro packages built locally by scripts/vmtest/build-deb.sh for VM testing.
dist/

# Nix build output symlinks (point into /nix/store; regenerated by `nix build`).
result
result-*

# Personal scratch notes.
TODO

# Python bytecode caches (the Python SDK in python/ and its pytest run).
__pycache__/
*.pyc
